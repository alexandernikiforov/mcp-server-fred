spring:
  ai:
    mcp:
      server:
        # building the server addressed over Streamable HTTP
        protocol: streamable
        type: async

fred:
  client:
    base-url: https://api.stlouisfed.org/
